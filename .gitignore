node_modules
dist
dist-ssr
*.local

.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local environment
.env
.env.local
.env.*.local

# Local project reference — deliberately not published.
# Kept out of the repo on request, and ignored so that a stray `git add .`
# cannot push it by accident.
PROJECT.md
