# Optional. Point this at the push backend in server/ to get reminders that
# arrive with the app fully closed. Leave it unset and reminders still work,
# but only while NutriTrack is open in a tab.
VITE_PUSH_SERVER=http://localhost:8787
